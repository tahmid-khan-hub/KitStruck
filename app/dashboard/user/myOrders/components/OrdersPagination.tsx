export default function OrdersPagination({ page, setPage, totalPages }: {
  page: number; setPage: (p: number) => void; totalPages: number;
}) {
    return (
        <div>
            
        </div>
    )
}